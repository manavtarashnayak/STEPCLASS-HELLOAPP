---

## UC2: Accept Name from Command-Line

### Description
The application accepts a name as a command-line argument and greets the user.

### Main Flow
1. Run the program with a name argument
2. The application reads the argument
3. Displays greeting with the provided name

### Example
Input:
java HelloApp Simon

Output:
Hello Simon